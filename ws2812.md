# WS2812 RGB LED 状态指示灯支持

本文档描述了为 debugprobe 添加 WS2812 RGB LED 状态指示灯的功能实现。

## 功能概述

通过 GP16 引脚连接一个 WS2812 RGB LED，用于显示 debugprobe 的各种运行状态，包括：

- USB 连接状态
- DAP 调试器连接状态
- DAP 数据传输状态
- UART 串口收发状态

## 硬件连接

| 引脚 | 功能 |
|------|------|
| GP16 | WS2812 数据线 (DIN) |
| 3.3V | WS2812 电源 (VCC) |
| GND  | WS2812 地线 (GND) |

## 状态颜色定义

| 状态 | 颜色 | GRB 值 | 说明 |
|------|------|--------|------|
| USB 断开 | 暗红色 | `0x000800` | USB 未连接或未就绪 |
| USB 已连接 | 暗绿色 | `0x080000` | USB 已连接，等待调试 |
| DAP 已连接 | 中等绿色 | `0x400000` | 调试器已建立 SWD 连接 |
| DAP 运行中 | 中等蓝色 | `0x000040` | 正在执行数据传输 |
| UART TX | 黄色 | `0x101000` | UART 正在发送数据 |
| UART RX | 青色 | `0x100010` | UART 正在接收数据 |
| UART TX+RX | 洋红色 | `0x001010` | UART 同时收发数据 |

### 颜色优先级（从高到低）

1. UART TX/RX 活动
2. DAP 运行中
3. DAP 已连接
4. USB 已连接
5. USB 断开

## 编译配置

在 `include/board_pico_config.h` 中添加以下定义以启用 WS2812 支持：

```c
// WS2812 LED support - GP16
#define WS2812_GPIO 16
#define PROBE_WS2812_SUPPORT
```

编译时需要使用 `DEBUG_ON_PICO=ON` 选项：

```bash
cd build
cmake .. -DDEBUG_ON_PICO=ON
make -j4
```

## 修改的文件

### 新增文件

| 文件 | 说明 |
|------|------|
| `src/ws2812_control.c` | WS2812 驱动实现，状态管理和颜色控制 |
| `src/ws2812_control.h` | WS2812 API 定义，状态枚举和颜色常量 |
| `src/ws2812.pio` | PIO 程序，用于生成 WS2812 时序信号 |

### 修改的文件

| 文件 | 修改内容 |
|------|----------|
| `CMakeLists.txt` | 添加 `ws2812_control.c` 源文件和 `ws2812.pio` 头文件生成 |
| `include/board_pico_config.h` | 添加 `WS2812_GPIO` 和 `PROBE_WS2812_SUPPORT` 定义 |
| `include/DAP_config.h` | 在 `PORT_SWD_SETUP()` 和 `PORT_OFF()` 中添加 DAP 连接状态控制 |
| `src/main.c` | 添加 `ws2812_init()` 初始化和 USB 状态更新 |
| `src/cdc_uart.c` | 添加 UART TX/RX 状态指示 |
| `src/tusb_edpt_handler.c` | 添加 DAP 运行状态指示 |

## 技术实现细节

### PIO 资源分配

- **pio0**: 用于 SWD 调试 (probe.pio)
- **pio1**: 用于 WS2812 LED 控制

> **重要**: WS2812 必须使用 `pio1`，避免与 `pio0` 上的 SWD 调试功能冲突。

### 状态管理

`ws2812_control.c` 使用状态标志数组管理多个并发状态，并根据优先级选择显示颜色：

```c
typedef enum {
    WS2812_STATE_USB_DISCONNECTED = 0,
    WS2812_STATE_USB_CONNECTED,
    WS2812_STATE_DAP_CONNECTED,
    WS2812_STATE_DAP_RUNNING,
    WS2812_STATE_UART_TX,
    WS2812_STATE_UART_RX,
    WS2812_STATE_MAX
} ws2812_state_t;
```

### API 接口

```c
// 初始化 WS2812
void ws2812_init(void);

// 设置状态
void ws2812_set_state(ws2812_state_t state, bool active);

// 直接设置颜色 (GRB 格式)
void ws2812_set_color(uint32_t color);

// 直接设置 RGB 颜色
void ws2812_set_rgb(uint8_t r, uint8_t g, uint8_t b);
```

## 引脚总览

完整的 debugprobe 引脚分配：

| 功能 | 引脚 |
|------|------|
| RESET | GP1 |
| SWCLK | GP2 |
| SWDIO | GP3 |
| UART TX | GP4 |
| UART RX | GP5 |
| WS2812 | GP16 |
| 板载 LED | GP25 |

## 禁用 WS2812 支持

如需禁用 WS2812 功能，只需注释掉 `board_pico_config.h` 中的定义：

```c
// #define WS2812_GPIO 16
// #define PROBE_WS2812_SUPPORT
```

重新编译后，固件将不包含 WS2812 相关代码。
